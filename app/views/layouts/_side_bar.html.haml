.side-bar
  - if user_signed_in?
    - if content_for?(:side_bar)
      = yield :side_bar
  - else
    .side-bar-wrapper
      .side-bar-sign-in
      .side-bar-register
    %hr
    %p.register-question Not a member yet?
    .btn.btn-default.side-bar-register-button Register
